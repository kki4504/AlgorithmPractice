const input = parseInt(require('fs').readFileSync('./dev/stdin').toString().trim());

let X = input;
let first = 0;
let second = 0;
let count = 0;

while (count < 10) {
  count += 1
  if(count < 2) {
    
  }

  if(input === X)
    break;
}
